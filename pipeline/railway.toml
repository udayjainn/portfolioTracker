# Railway: set service Root Directory to the repository root (not /pipeline).
[build]
builder = "DOCKERFILE"
dockerfilePath = "pipeline/Dockerfile"

[deploy]
startCommand = "sh pipeline/scripts/start-worker.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
